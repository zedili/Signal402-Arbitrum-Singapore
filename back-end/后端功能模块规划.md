# X402AiPolyMarket 后端功能模块规划文档

## 📋 项目概述

**项目名称**: X402AiPolyMarket - AI驱动的预测市场平台  
**技术栈**: 
- 后端: Go + go-zero 微服务框架
- 前端: Next.js + React + TypeScript
- 智能合约: Solidity + Hardhat + OpenZeppelin
- 数据库: (待添加) MySQL/PostgreSQL + Redis

**项目定位**: 基于区块链和AI技术的去中心化预测市场平台，用户可以对各类事件（加密货币、科技、股票、政治等）进行预测和交易。

---

## 🎯 核心业务流程分析

### 前端已实现功能
1. ✅ 市场展示页面（分类筛选、搜索）
2. ✅ 市场卡片（AI预测、置信度、YES/NO价格）
3. ✅ 用户界面布局（导航、钱包连接）
4. ✅ 主题切换、响应式设计

### 智能合约已实现
1. ✅ InsightToken (ERC20代币)
   - 代币铸造和管理
   - 服务费折扣支付功能
   - 平台金库管理
   - 价格预言机接口（待实现）

### 后端现状
- ⚠️ 仅有基础脚手架代码
- ⚠️ 只有一个示例接口 `/from/:name`
- ⚠️ 缺少数据库配置
- ⚠️ 缺少业务逻辑实现

---

## 🏗️ 后端功能模块架构设计

### 模块一：用户管理模块 (User Module)

#### 1.1 用户认证与授权
**功能点**:
- 钱包地址登录（Web3认证）
- JWT Token生成与验证
- 用户会话管理
- 签名验证（MetaMask/WalletConnect）

**API接口**:
```
POST   /api/v1/auth/login          # 钱包登录
POST   /api/v1/auth/verify         # 验证签名
POST   /api/v1/auth/refresh        # 刷新Token
POST   /api/v1/auth/logout         # 登出
```

**数据表设计**:
```sql
users (
  id BIGINT PRIMARY KEY,
  wallet_address VARCHAR(42) UNIQUE,
  username VARCHAR(50),
  avatar_url VARCHAR(255),
  email VARCHAR(100),
  created_at TIMESTAMP,
  updated_at TIMESTAMP,
  last_login_at TIMESTAMP,
  status TINYINT  -- 0:正常 1:禁用
)
```

#### 1.2 用户资料管理
**功能点**:
- 用户信息查询
- 用户资料更新
- 用户统计数据（交易次数、胜率、收益等）

**API接口**:
```
GET    /api/v1/user/profile        # 获取用户资料
PUT    /api/v1/user/profile        # 更新用户资料
GET    /api/v1/user/stats          # 获取用户统计
GET    /api/v1/user/:address       # 查询其他用户信息
```

---

### 模块二：市场管理模块 (Market Module)

#### 2.1 市场创建与管理
**功能点**:
- 创建预测市场
- 市场信息编辑
- 市场状态管理（进行中、已结束、已结算）
- 市场分类管理
- 市场审核机制

**API接口**:
```
POST   /api/v1/market/create       # 创建市场
GET    /api/v1/market/list         # 市场列表（分页、筛选）
GET    /api/v1/market/:id          # 市场详情
PUT    /api/v1/market/:id          # 更新市场信息
DELETE /api/v1/market/:id          # 删除市场（管理员）
GET    /api/v1/market/categories   # 获取分类列表
GET    /api/v1/market/hot          # 热门市场
GET    /api/v1/market/trending     # 趋势市场
```

**数据表设计**:
```sql
markets (
  id BIGINT PRIMARY KEY,
  question TEXT NOT NULL,
  description TEXT,
  category VARCHAR(50),
  creator_address VARCHAR(42),
  contract_address VARCHAR(42),  -- 对应的智能合约地址
  
  -- 价格信息
  yes_price DECIMAL(10,2),
  no_price DECIMAL(10,2),
  
  -- 统计信息
  total_volume DECIMAL(20,2),
  total_liquidity DECIMAL(20,2),
  participant_count INT,
  
  -- 时间信息
  start_time TIMESTAMP,
  end_time TIMESTAMP,
  settlement_time TIMESTAMP,
  
  -- 状态
  status TINYINT,  -- 0:待开始 1:进行中 2:已结束 3:已结算 4:已取消
  result TINYINT,  -- NULL:未结算 0:NO 1:YES
  
  -- 标签
  is_hot BOOLEAN DEFAULT FALSE,
  is_featured BOOLEAN DEFAULT FALSE,
  
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)
```

#### 2.2 市场数据统计
**功能点**:
- 实时价格更新
- 交易量统计
- 参与人数统计
- 历史价格曲线

**API接口**:
```
GET    /api/v1/market/:id/stats    # 市场统计数据
GET    /api/v1/market/:id/chart    # 价格历史图表数据
GET    /api/v1/market/:id/trades   # 最近交易记录
```

---

### 模块三：交易模块 (Trading Module)

#### 3.1 订单管理
**功能点**:
- 创建买入/卖出订单
- 订单撮合
- 订单历史查询
- 订单取消

**API接口**:
```
POST   /api/v1/trade/order         # 创建订单
GET    /api/v1/trade/orders        # 用户订单列表
GET    /api/v1/trade/order/:id     # 订单详情
DELETE /api/v1/trade/order/:id     # 取消订单
GET    /api/v1/trade/history       # 交易历史
```

**数据表设计**:
```sql
orders (
  id BIGINT PRIMARY KEY,
  market_id BIGINT,
  user_address VARCHAR(42),
  
  order_type TINYINT,  -- 0:买入 1:卖出
  position TINYINT,    -- 0:NO 1:YES
  
  amount DECIMAL(20,8),
  price DECIMAL(10,2),
  total_value DECIMAL(20,8),
  
  filled_amount DECIMAL(20,8),
  status TINYINT,  -- 0:待成交 1:部分成交 2:完全成交 3:已取消
  
  tx_hash VARCHAR(66),  -- 区块链交易哈希
  
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)

trades (
  id BIGINT PRIMARY KEY,
  market_id BIGINT,
  buy_order_id BIGINT,
  sell_order_id BIGINT,
  
  buyer_address VARCHAR(42),
  seller_address VARCHAR(42),
  
  position TINYINT,
  amount DECIMAL(20,8),
  price DECIMAL(10,2),
  
  tx_hash VARCHAR(66),
  block_number BIGINT,
  
  created_at TIMESTAMP
)
```

#### 3.2 持仓管理
**功能点**:
- 用户持仓查询
- 持仓盈亏计算
- 持仓历史记录

**API接口**:
```
GET    /api/v1/position/list       # 用户持仓列表
GET    /api/v1/position/:marketId  # 特定市场持仓
GET    /api/v1/position/pnl        # 盈亏统计
```

**数据表设计**:
```sql
positions (
  id BIGINT PRIMARY KEY,
  user_address VARCHAR(42),
  market_id BIGINT,

  position TINYINT,  -- 0:NO 1:YES
  shares DECIMAL(20,8),
  avg_price DECIMAL(10,2),
  total_cost DECIMAL(20,8),

  current_value DECIMAL(20,8),
  unrealized_pnl DECIMAL(20,8),

  updated_at TIMESTAMP
)
```

---

### 模块四：AI预测模块 (AI Prediction Module)

#### 4.1 AI预测服务
**功能点**:
- 集成AI模型进行市场预测
- 预测结果存储与更新
- 预测置信度计算
- 预测历史追踪

**API接口**:
```
GET    /api/v1/ai/prediction/:marketId    # 获取AI预测
POST   /api/v1/ai/analyze                 # 触发AI分析
GET    /api/v1/ai/history/:marketId       # 预测历史
GET    /api/v1/ai/accuracy                # AI准确率统计
```

**数据表设计**:
```sql
ai_predictions (
  id BIGINT PRIMARY KEY,
  market_id BIGINT,

  prediction_value DECIMAL(5,2),  -- 0-100
  confidence DECIMAL(5,2),         -- 0-100
  suggests TINYINT,                -- 0:NO 1:YES

  model_version VARCHAR(50),
  analysis_data JSON,              -- AI分析的详细数据

  created_at TIMESTAMP
)
```

#### 4.2 数据源管理
**功能点**:
- 外部数据源接入（新闻、社交媒体、链上数据）
- 数据清洗与预处理
- 数据更新调度

**API接口**:
```
GET    /api/v1/datasource/list     # 数据源列表
POST   /api/v1/datasource/sync     # 同步数据
GET    /api/v1/datasource/status   # 数据源状态
```

---

### 模块五：钱包与资产模块 (Wallet Module)

#### 5.1 钱包管理
**功能点**:
- 用户余额查询（链上+链下）
- 充值记录
- 提现记录
- 资产冻结/解冻

**API接口**:
```
GET    /api/v1/wallet/balance      # 查询余额
GET    /api/v1/wallet/deposits     # 充值记录
GET    /api/v1/wallet/withdrawals  # 提现记录
POST   /api/v1/wallet/withdraw     # 申请提现
GET    /api/v1/wallet/transactions # 交易流水
```

**数据表设计**:
```sql
wallet_balances (
  id BIGINT PRIMARY KEY,
  user_address VARCHAR(42) UNIQUE,

  usdc_balance DECIMAL(20,8),
  token_balance DECIMAL(20,8),
  frozen_balance DECIMAL(20,8),

  updated_at TIMESTAMP
)

wallet_transactions (
  id BIGINT PRIMARY KEY,
  user_address VARCHAR(42),

  tx_type TINYINT,  -- 0:充值 1:提现 2:交易 3:奖励 4:手续费
  amount DECIMAL(20,8),
  currency VARCHAR(10),

  balance_before DECIMAL(20,8),
  balance_after DECIMAL(20,8),

  related_id BIGINT,  -- 关联的订单/市场ID
  tx_hash VARCHAR(66),

  status TINYINT,  -- 0:待处理 1:成功 2:失败
  remark TEXT,

  created_at TIMESTAMP
)
```

#### 5.2 代币服务费支付
**功能点**:
- InsightToken支付AI服务费
- 折扣计算
- 支付记录

**API接口**:
```
POST   /api/v1/payment/ai-service  # 支付AI服务费
GET    /api/v1/payment/discount    # 查询折扣信息
GET    /api/v1/payment/history     # 支付历史
```

---

### 模块六：结算模块 (Settlement Module)

#### 6.1 市场结算
**功能点**:
- 市场结果确认
- 自动结算触发
- 收益分配
- 结算争议处理

**API接口**:
```
POST   /api/v1/settlement/settle/:marketId  # 触发结算
GET    /api/v1/settlement/status/:marketId  # 结算状态
GET    /api/v1/settlement/disputes          # 争议列表
POST   /api/v1/settlement/dispute           # 提交争议
```

**数据表设计**:
```sql
settlements (
  id BIGINT PRIMARY KEY,
  market_id BIGINT,

  result TINYINT,  -- 0:NO 1:YES
  total_pool DECIMAL(20,8),
  winner_pool DECIMAL(20,8),
  loser_pool DECIMAL(20,8),

  settlement_ratio DECIMAL(10,6),
  platform_fee DECIMAL(20,8),

  settled_at TIMESTAMP,
  tx_hash VARCHAR(66)
)

user_settlements (
  id BIGINT PRIMARY KEY,
  settlement_id BIGINT,
  user_address VARCHAR(42),

  position TINYINT,
  shares DECIMAL(20,8),
  cost DECIMAL(20,8),

  payout DECIMAL(20,8),
  profit DECIMAL(20,8),

  claimed BOOLEAN DEFAULT FALSE,
  claim_tx_hash VARCHAR(66),
  claimed_at TIMESTAMP
)
```

---

### 模块七：排行榜与统计模块 (Leaderboard Module)

#### 7.1 排行榜
**功能点**:
- 收益排行榜
- 胜率排行榜
- 交易量排行榜
- 周/月/总榜

**API接口**:
```
GET    /api/v1/leaderboard/profit      # 收益排行
GET    /api/v1/leaderboard/winrate     # 胜率排行
GET    /api/v1/leaderboard/volume      # 交易量排行
GET    /api/v1/leaderboard/user/:addr  # 用户排名
```

#### 7.2 平台统计
**功能点**:
- 平台总交易量
- 活跃用户数
- 市场数量统计
- AI准确率统计

**API接口**:
```
GET    /api/v1/stats/platform       # 平台总览
GET    /api/v1/stats/markets        # 市场统计
GET    /api/v1/stats/users          # 用户统计
GET    /api/v1/stats/ai             # AI统计
```

---

### 模块八：通知与消息模块 (Notification Module)

#### 8.1 系统通知
**功能点**:
- 交易通知
- 市场结算通知
- 系统公告
- 个人消息

**API接口**:
```
GET    /api/v1/notification/list    # 通知列表
PUT    /api/v1/notification/:id/read # 标记已读
DELETE /api/v1/notification/:id    # 删除通知
GET    /api/v1/notification/unread  # 未读数量
```

**数据表设计**:
```sql
notifications (
  id BIGINT PRIMARY KEY,
  user_address VARCHAR(42),

  type TINYINT,  -- 0:系统 1:交易 2:结算 3:个人
  title VARCHAR(200),
  content TEXT,

  related_type VARCHAR(50),
  related_id BIGINT,

  is_read BOOLEAN DEFAULT FALSE,
  read_at TIMESTAMP,

  created_at TIMESTAMP
)
```

---

### 模块九：管理后台模块 (Admin Module)

#### 9.1 市场管理
**功能点**:
- 市场审核
- 市场编辑
- 强制结算
- 市场下架

**API接口**:
```
GET    /api/v1/admin/markets/pending    # 待审核市场
PUT    /api/v1/admin/market/:id/approve # 审核通过
PUT    /api/v1/admin/market/:id/reject  # 审核拒绝
POST   /api/v1/admin/market/:id/settle  # 强制结算
```

#### 9.2 用户管理
**功能点**:
- 用户列表查询
- 用户封禁/解封
- 用户数据统计

**API接口**:
```
GET    /api/v1/admin/users          # 用户列表
PUT    /api/v1/admin/user/:id/ban   # 封禁用户
PUT    /api/v1/admin/user/:id/unban # 解封用户
GET    /api/v1/admin/user/:id/detail # 用户详情
```

#### 9.3 系统配置
**功能点**:
- 平台参数配置
- 手续费配置
- AI模型配置

**API接口**:
```
GET    /api/v1/admin/config         # 获取配置
PUT    /api/v1/admin/config         # 更新配置
```

---

## 🔧 技术实现要点

### 1. 数据库设计
**推荐方案**: MySQL 8.0 + Redis
- MySQL: 存储业务数据
- Redis: 缓存热点数据、实时价格、排行榜

### 2. 区块链交互
**实现方式**:
- 使用 go-ethereum 库与智能合约交互
- 监听链上事件（订单、结算等）
- 交易签名与验证

### 3. AI集成
**实现方式**:
- 调用外部AI服务API（OpenAI、Claude等）
- 或部署自己的AI模型服务
- 定时任务更新预测结果

### 4. 实时数据推送
**实现方式**:
- WebSocket连接
- 价格实时更新
- 订单状态推送

### 5. 安全机制
- JWT认证
- 签名验证
- 接口限流
- SQL注入防护
- XSS防护

---

## 📅 开发优先级建议

### 第一阶段（核心功能）
1. ✅ 用户认证模块（钱包登录）
2. ✅ 市场管理模块（创建、查询、列表）
3. ✅ 交易模块（订单、持仓）
4. ✅ 钱包模块（余额查询）

### 第二阶段（增强功能）
5. ✅ AI预测模块
6. ✅ 结算模块
7. ✅ 排行榜模块
8. ✅ 通知模块

### 第三阶段（管理功能）
9. ✅ 管理后台
10. ✅ 数据统计与分析
11. ✅ 系统监控

---

## 📦 依赖包建议

```go
// go.mod 需要添加的依赖
require (
    github.com/zeromicro/go-zero v1.9.4
    github.com/ethereum/go-ethereum v1.13.0  // 区块链交互
    gorm.io/gorm v1.25.0                     // ORM
    gorm.io/driver/mysql v1.5.0              // MySQL驱动
    github.com/go-redis/redis/v8 v8.11.5     // Redis
    github.com/golang-jwt/jwt/v4 v4.5.2      // JWT
    github.com/gorilla/websocket v1.5.0      // WebSocket
)
```

---

## 🎯 总结

这份规划涵盖了一个完整的AI预测市场平台所需的所有核心功能模块。建议按照优先级逐步实现，先完成核心交易流程，再添加增强功能。

**关键点**:
1. 前后端数据结构要保持一致
2. 智能合约事件要与后端数据同步
3. 实时性要求高的数据使用Redis缓存
4. 安全性是重中之重，特别是涉及资金的操作


